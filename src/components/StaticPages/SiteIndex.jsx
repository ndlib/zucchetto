'use strict'
var React = require('react');

var SiteIndex = React.createClass({

  render: function() {
    return (
      <div>
        INDEX PAGE
      </div>
    );
  }

});

module.exports = SiteIndex;
